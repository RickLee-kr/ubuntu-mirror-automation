#!/usr/bin/env bash
# Canonical name alias — implementation remains in dp-preflight-common.sh
# shellcheck source=scripts/lib/dp-preflight-common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/dp-preflight-common.sh"
